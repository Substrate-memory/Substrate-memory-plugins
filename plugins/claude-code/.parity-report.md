# Claude Code parity report (plugins/claude-code)

## Files created (nothing else touched)

- `plugins/claude-code/.claude-plugin/plugin.json` — native manifest
  (`substrate-memory` 0.3.0).
- `plugins/claude-code/.mcp.json` — MCP stdio server registration.
- `plugins/claude-code/hooks/hooks.json` + `hooklib.py`,
  `user_prompt_submit.py`, `session_start.py`, `stop.py`, `session_end.py`,
  `capture_worker.py`.
- `plugins/claude-code/mcp_server.py` — stdlib MCP stdio server (3 tools).
- `plugins/claude-code/runtime.py` — host-neutral port of
  `plugins/substrate/plugin.py` (minus Hermes `register()`; plus
  `get_turn_context` / `queue_turn_capture` / `queue_session_boundary` /
  `flush_capture`).
- `plugins/claude-code/substrate_core/` — byte-identical `client.py`,
  `contract.py`, `onboarding.py`, `ca/*.pem`, `contract/envelope-fixtures.json`
  plus `hosthome.py` and `__init__.py`. Only marked `HOST-HOME PATCH` hunks
  differ (home resolution; one user-facing "Claude Code profile" string).
- `plugins/claude-code/connect.py`, `commands/substrate-connect.md`,
  `skills/substrate-memory/SKILL.md`, `README.md`, `INSTALL.md`.
- `.claude-plugin/marketplace.json` (repo root, owned by this agent) —
  minimal entry for `substrate-memory` only (`$schema` omitted: the
  `anthropic.com` host trips `scripts/check_public_hygiene.py`).
- `tests/test_claude_code_plugin.py` — 24 offline tests.

## Install command

```sh
claude plugin marketplace add https://github.com/Substrate-memory/Substrate-memory-plugins
claude plugin install substrate-memory@substrate-marketplace
```

Verified end-to-end in a sandboxed `$HOME` against `claude` 2.1.259:
marketplace add (repo root path) rc 0, install rc 0, status enabled,
`plugin details` shows 4 hooks + 1 MCP server + 2 skills.

## Exact test results

- `pytest -q tests/test_claude_code_plugin.py`: **24 passed**.
- `ruff check plugins/claude-code tests/test_claude_code_plugin.py`: **clean**.
- `claude plugin validate --strict plugins/claude-code`: **pass**;
  `claude plugin validate .claude-plugin/marketplace.json`: **pass**.
- `python3 scripts/check_public_hygiene.py --root .`: **pass**.
- Full suite `pytest -q`: **175 passed, 4 failed** — all 4 failures are in
  sibling files (`test_grok_bot_plugin.py::test_retrieval_failure_returns_empty_context`,
  3x `test_openclaw_plugin.py`) and reproduce with my file `--ignore`d, so
  they are pre-existing and unrelated to this plugin.
- Live smoke: MCP stdio `initialize → tools/list → tools/call memory_search`
  over real pipes rc 0; hook scripts exit 0 offline (fail closed).

## Parity gaps

- None functional. Static prompt rides `SessionStart.additionalContext`
  (Claude Code has no system-prompt-section API) plus skill text.
- `turn` in pre-turn requests derives from transcript history when available,
  else 0; `turn_id` falls back to uuid4 exactly like the Hermes fallback.
- Stop-hook capture parses the JSONL transcript (user/assistant text only —
  no tool results, no thinking blocks), matching the Hermes privacy boundary.

## For the integration agent

- Root README: add the Claude Code install stanza
  (`marketplace add <repo-url>` + `install substrate-memory@substrate-marketplace`)
  and the approval-URL onboarding note.
- CI: add `claude plugin validate` for `plugins/claude-code` and the root
  marketplace file if the CLI is available in CI.
- Marketplace: merge this minimal `.claude-plugin/marketplace.json` with the
  other hosts' entries (keep `source: ./plugins/<host>` style).
- Test isolation: every host vendors top-level `substrate_core`; test files
  must evict `sys.modules['substrate_core*']` + restore `sys.path` (this
  file does) or sibling suites collide on import order.
- Sibling failures to triage: the 4 listed above (openclaw timeout-error
  mapping, static-prompt/diff assertions; grok-bot empty-context test).
