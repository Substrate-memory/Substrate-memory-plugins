# CODEX worker report — plugins/substrate-codex

## Files changed (committed as a573264, signed off)
- `plugins/substrate-codex/plugin.json` — portable manifest ($schema agent-plugins 1.0.0, extensions.com.openai with interface + hooks ./hooks/hooks.json)
- `plugins/substrate-codex/.codex-plugin/plugin.json` — Codex compat manifest (skills ./skills/, mcpServers ./.mcp.json, full interface; NO hooks field)
- `plugins/substrate-codex/mcp.json` + `.mcp.json` — substrate-memory, streamable-http, https://app.trysubstrate.co/mcp
- `plugins/substrate-codex/hooks/hooks.json` — UserPromptSubmit, PostToolUse, Stop, SubagentStop, SessionStart (startup|resume|clear|compact), PreCompact; platform codex, turn_id pass-through, 5 s timeouts; no SessionEnd
- `plugins/substrate-codex/skills/{substrate-memory,substrate-connect,substrate-sync,substrate-import}/SKILL.md`
- `plugins/substrate-codex/scripts/substrate_sync.py` — byte-identical copy of claude canonical (sha256 fed1fad0...)
- `plugins/substrate-codex/README.md`, `INSTALL.md`
- `.agents/plugins/marketplace.json` — NEW, substrate-marketplace / Substrate Memory / substrate-codex AVAILABLE / ON_USE / Productivity
- `tests/test_substrate_codex_plugin.py` — 8 tests

## Test commands and results
- `uv run --frozen --extra dev python -m pytest -q tests/test_substrate_codex_plugin.py` → 8 passed
- `/usr/bin/python3 ~/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py plugins/substrate-codex` → passed (note: run OUTSIDE `uv run`; the venv python lacks yaml, so the test pins /usr/bin/python3)
- Sandbox: `CODEX_HOME=/tmp/sb-codex codex plugin marketplace add <repo>` → ok; `codex plugin add substrate-codex@substrate-marketplace` → ok; `codex plugin list --json` shows installed/enabled, AVAILABLE/ON_USE. Debug `codex exec` log shows `mcp_servers="substrate-memory"` and no hook parse warnings.

## Verified vs assumed
- VERIFIED (CLI 0.154.0 sandbox): marketplace add/install/list round-trip; bundled MCP server registers under bare key `substrate-memory`; hooks `server: "substrate-memory"` matches docs example shape; no hook warnings in debug log.
- VERIFIED (validator source): `.codex-plugin/plugin.json` must NOT contain `hooks` (rejected); default `hooks/hooks.json` discovery applies instead. Portable root manifest carries the hooks pointer.
- VERIFIED (docs fetch): SessionEnd has no mcp_tool support (omitted); SubagentStop/UserPromptSubmit/Stop/PostToolUse/SessionStart/PreCompact fields per https://developers.openai.com/codex/hooks.md.
- ASSUMED (could not fully verify without interactive auth): hook trust flow via /hooks works as documented; `codex mcp login` browser flow; ChatGPT/Codex app Plugins-directory install from GitHub marketplace.

## Open questions
- `.agents/plugins/marketplace.json` currently lists only substrate-codex. If substrate-claude needs a repo-marketplace entry too, the two entries must be merged into one file (coordinate to avoid overwrite).
- SubagentStop sends both `session_id` and `parent_session_id` = `${session_id}`; server routes subagent turns by `parent_session_id`. Confirm server accepts identical values.
- `.mcp.json` uses `streamable-http` (same as portable `mcp.json`); substrate-mcp legacy file uses `http`. Confirm CLI accepts `streamable-http` from plugin-bundled `.mcp.json` on all surfaces.
