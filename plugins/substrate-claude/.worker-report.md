# Worker report: plugins/substrate-claude (0.7.0)

One package serves Claude Cowork (cloud and local sessions) and Claude Code.

## Files changed (all under my ownership, plus one additive marketplace entry)

- `plugins/substrate-claude/.claude-plugin/plugin.json` — name substrate-claude,
  version 0.7.0, author Sightline Technologies Inc, description, homepage,
  license MIT, keywords. (No `category` key: `claude plugin validate --strict`
  rejects it in plugin.json; category lives in the marketplace entry.)
- `plugins/substrate-claude/.mcp.json` — remote HTTP server `substrate-memory`
  at `https://app.trysubstrate.co/mcp`.
- `plugins/substrate-claude/hooks/hooks.json` — contract section 7 exactly:
  UserPromptSubmit to memory_turn_context; PostToolUse (no matcher) to
  memory_capture_tool; Stop to memory_capture_turn (main); SubagentStop to
  memory_capture_turn (subagent, agent_id, parent_session_id=${session_id});
  SessionStart matchers startup|resume|clear|compact to memory_session_boundary
  with boundary=${source}; PreCompact to boundary compact; SessionEnd to
  boundary end with reason. Server `plugin:substrate-claude:substrate-memory`,
  platform `claude`, timeouts 5 s (SessionEnd 3 s).
- `plugins/substrate-claude/skills/substrate-memory/SKILL.md` — static memory
  prompt semantics, self-check protocol, catch-up rule, no-secrets rule,
  forget semantics, no over-claiming.
- `plugins/substrate-claude/commands/substrate-connect.md`,
  `substrate-sync.md`, `substrate-import.md` — agent runbooks per contract
  section 8 (memory_import_status, scripts/substrate_sync.py, memory_import).
- `plugins/substrate-claude/scripts/substrate_sync.py` — stdlib only,
  Python >= 3.9, host-neutral; --host claude|codex, --list, --session,
  --after-index, --origin; section 6 redaction; capture_turn envelopes per
  CONTRACT.md section 5; batching <= 64 items / <= 240 KiB, one JSON batch
  per line. Byte-identical with
  `plugins/substrate-codex/scripts/substrate_sync.py` (sha256 `fed1fad0...`,
  acked by sibling plugin-codex; that copy is canonical-shared).
- `plugins/substrate-claude/README.md`, `INSTALL.md` — customer-facing docs
  and agent runbook (Cowork marketplace flow, Claude Code slash-command flow,
  sign-in flow, capture/privacy/limits/troubleshooting).
- `tests/test_substrate_claude_plugin.py` + `tests/fixtures/substrate-claude/`
  (Claude transcript + Codex rollout fixtures) — manifest/hook/mcp shape,
  every hook maps to the contract table, script output validates with
  `substrate.contract.validate_envelope`, shared redaction fixture passes,
  deterministic ids.
- `.claude-plugin/marketplace.json` — ADDED substrate-claude 0.7.0 entry only;
  existing substrate-mcp entry untouched.

## Test commands and results (all pass)

- `uv run --frozen --extra dev python -m pytest -q tests/test_substrate_claude_plugin.py`
  from the repo root: **12 passed**.
- `claude plugin validate --strict plugins/substrate-claude` (CLI 2.1.263):
  **Validation passed**.
- Sandbox proof (`HOME=/tmp/sb-claude CLAUDE_CONFIG_DIR=/tmp/sb-claude/.claude`):
  marketplace add, install `-s user -y`, and `claude plugin details
  substrate-claude` all succeed; details list Skills (4),
  Hooks (7: UserPromptSubmit, PostToolUse, Stop, SubagentStop, SessionStart,
  PreCompact, SessionEnd), MCP servers (1: substrate-memory). Real home
  untouched; no OAuth or model turns attempted.
- Script smoke-tested read-only against real `~/.claude/projects` and
  `~/.codex/sessions`: parses, redacts, envelopes validate.

## Verified vs unverified

- Verified: manifest/mcp/hooks shapes, hook-to-contract mapping, script
  parsing/redaction/envelope validity/batching/determinism, strict plugin
  validation, sandbox marketplace install with hooks/MCP/skills/commands
  registered.
- Unverified (needs credentials / live session, deliberately not attempted):
  real OAuth browser flow, live `memory_*` tool calls, Cowork cloud install
  click-path, SessionStart-at-launch skip behavior on a real host.

## Open questions

- None blocking. Coordination note: `substrate_sync.py` is shared
  byte-identically with the codex worker; future changes must update both
  copies together (sibling acked this protocol).
